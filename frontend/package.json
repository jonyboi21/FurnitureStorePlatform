{
  "name": "bookstore-app",
  "version": "1.0.0",
  "description": "A simple bookstore application built with React.",
  "author": "Your Name",
  "license": "MIT",

  "engines": {
    "node": ">=20 <25",
    "npm": ">=8"
  },

  "scripts": {
    "start":  "react-scripts start",               
    "build":  "react-scripts build",               
    "test":   "react-scripts test",
    "eject":  "react-scripts eject",

    "lint":   "eslint 'src/**/*.{js,jsx}'",
    "deploy": "npm run build && aws s3 sync build/ s3://YOURâ€‘BUCKETâ€‘NAME --delete --region YOURâ€‘REGION"
  },

  "dependencies": {
    "axios": "^1.10.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^5.2.0",
    "react-scripts": "^5.0.1"
  },

  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-plugin-react": "^7.27.1"
  },

  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },

  "keywords": [
    "react",
    "bookstore",
    "application"
  ]
}